CREATE TABLE sessions (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    serialized JSONB,
    roles JSONB 
);

CREATE TABLE trunk (
    id SERIAL PRIMARY KEY,
    value JSONB
);

INSERT INTO trunk (id, value) VALUES (1, "{}");
